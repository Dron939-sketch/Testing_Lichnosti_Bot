services:
  - type: web
    name: variatica-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_adaptive.py
    envVars:
      - key: ENVIRONMENT
        value: production
