services:
  - type: web
    name: tarot-adaptive-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_adaptive.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
