services:
  - type: web
    name: variatica-bot
    env: python
    region: frankfurt
    buildCommand: |
      pip install -r requirements.txt
      # Создаем симлинк для обратной совместимости
      ln -sf app.py simple_server.py || true
    startCommand: python app.py
    envVars: ...
